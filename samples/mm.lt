use core

fn main() {
    core::printBang()

    let i: int = 0
    loop {
        if i > 3 {
            break
        }
        core::printInt(i)
        i += 1
        loop {
            i += 1
            break
        }
    }

    core::printDot()
}
