extern putchar(x)
extern cos(x)

fn print_dot() {
    putchar(46)
    putchar(10)
}

fn print_bang() {
    putchar(33)
    putchar(10)
}

fn foo(x) {
    let xasdf: u64 = 1
    2
}

fn main() {
    let x: u64 = 1
    let y: f64 = 1.0
    foo(2 + x)
    print_dot()
    print_bang()
}
