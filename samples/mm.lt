use core

fn main() {
    core::printBang()

    let i: int = 0
    while i < 3 {
        core::printInt(i)
        i += 1
    }

    // loop {
    //     if i < 3 {
    //         core::printInt(i)
    //         i += 1
    //     } else {
    //         break
    //     }
    // }

    core::printDot()
}
