use core

fn main() {
    core::printBang()

    let arr: core::Array
    arr.init()
    arr.push(9)
    core::printInt(arr.get(10))

    let foo: core::String = "mitral"
    printInt(foo.buffer[0])

    core::printDot()
}
